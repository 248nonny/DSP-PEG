[package]
name = "userspace"
version = "0.0.1"
edition.workspace = true

[dependencies]
eframe = "0.33.2"
libc = "0.2"
common = { path = "../lib/common" }

[features]
default = ["rpi"]
rpi = []
remote_linux = []
remote_windows = []
