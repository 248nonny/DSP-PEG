[package]
name = "dsp-core"
version = "0.0.1"
edition.workspace = true

[features]
default = []
# For building with std, either for testing or for using std-specific parts of the api (if any exist).
std = []
host = ["std"]

[dependencies]
const_format = "0.2.35"
